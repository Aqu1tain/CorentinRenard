import React from 'react';
import './Lastprojects.scss';

export default function Lastprojects() {

    return (
        <div className="lastprojects">
            <h1 className="title">Derniers projets</h1>
            {
                // GET LAST PROJECTS FROM API
            }
        </div>
    );
}